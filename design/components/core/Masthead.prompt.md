Top-of-page masthead used on every screen; the active item is underlined text, not a link.
```jsx
<Masthead active="Signals" items={[{label:'Signals',href:'#'},{label:'Reports',href:'#'},{label:'Sources',href:'#'},{label:'Cities',href:'#'}]} onSearch={openPalette} />
```
