Collection health indicator. Square, not round (stage badges are round). Never hide a failure — the label states the mechanism.
```jsx
<HealthDot state="fail" label="FAILING · feed truncated since W33" />
```
