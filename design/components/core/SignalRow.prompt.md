The registry's core row. The † dagger auto-renders when editorial exceeds adoption by >= 2 — never suppress it.
```jsx
<SignalRow signal={{name:'Quiet luxury', stage:'saturated', evidence:96, cities:['Milan','New York'], edi:2, ado:5, sat:5, span:'88 wks'}} />
```
