/** Oxblood notice band: since-last-visit digests, collection alerts. One line, hairline below. */
export interface AlertBandProps {
  /** uppercase lead, e.g. "Collection alert" */ label: string;
  children: React.ReactNode;
}
export declare function AlertBand(props: AlertBandProps): JSX.Element;
