# Deploy-time deps for the Vercel web viewer ONLY.
# The pipeline's deps (feedparser, trafilatura, yt-dlp, anthropic, ...) live in
# pyproject.toml and are never imported by app/main.py or culture.web.*, so
# they intentionally stay out of the serverless bundle. Versions match what
# uv resolved locally so dev and deploy behave identically.
fastapi==0.141.1
jinja2==3.1.6
markdown==3.10.3
sqlalchemy==2.0.52
psycopg[binary]==3.3.4
psycopg2-binary==2.9.12
pydantic==2.13.4
pydantic-settings==2.15.0
itsdangerous==2.2.0
resend==2.40.1
python-multipart==0.0.32
